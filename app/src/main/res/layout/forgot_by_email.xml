<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">
    <EditText
        android:id="@+id/emailForgot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="20dp"
        android:autoText="false"
        android:background="@drawable/test"
        android:ems="10"
        android:hint="@string/reg_email"
        android:inputType="textEmailAddress"
        android:textColor="@android:color/holo_orange_dark"
        android:textColorHint="?android:attr/colorButtonNormal"
        android:textColorLink="@color/blue"
        android:textSize="13dp">
        <requestFocus />
    </EditText>
    <Button
        android:id="@+id/nextbyemail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:background="@drawable/button"
        android:text="@string/forgot_next"
        android:textColorLink="@color/blue"
        android:textSize="13dp"
        android:layout_below="@+id/emailForgot"
        android:layout_alignStart="@+id/emailForgot"
        android:layout_alignEnd="@+id/emailForgot" />
</RelativeLayout>